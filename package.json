{
  "name": "moodle-ai-assistant",
  "version": "0.2.0",
  "private": true,
  "description": "Moodle AI assistant extension build tools",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
